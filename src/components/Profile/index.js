import React from 'react';

import Profile from "components/Profile/Profile";

const Index = () => {
    return (
        <div style={{maxWidth: 1300, margin: 'auto', padding: 8, background: '#fff', marginTop: -32}}>

            <Profile/>

        </div>
    );
};

export default Index;
